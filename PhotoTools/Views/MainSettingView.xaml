<UserControl x:Class="PhotoTools.Views.MainSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PhotoTools.Views"
             xmlns:settings="clr-namespace:PhotoTools.Views.Settings"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0">
            <Button x:Name="BtTabSettingTheme" Content="Theme" Click="BtTabItemTheme_OnClick"/>
            <Button x:Name="BtTabSettingLanguage" Content="Langue" Click="BtTabItemLanguage_OnClick"/>
        </StackPanel>
        <TabControl Grid.Column="1">
            <TabItem x:Name="TabLanguage" Visibility="Collapsed">
                <settings:Language/>
            </TabItem>
            <TabItem x:Name="TabTheme" Visibility="Collapsed">
                <settings:Theme/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
